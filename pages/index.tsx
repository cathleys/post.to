import { PageContainer, HeroSection } from "@features/index";
import { ArticleList } from "@features/ui";

const Home = () => {
  return (
    <PageContainer>
      <HeroSection />
      <ArticleList />
    </PageContainer>
  );
};

export default Home;
